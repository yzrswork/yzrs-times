name: Publish Midday Edition

on:
  schedule:
    # 発行 12:00 JST（Actions cronは遅延するため、昼休みの紙面到着を狙う）
    - cron: '0 3 * * *'
    # 補習発行 13:00 JST — その日の昼刊が未発行の場合のみ発行される（冪等）
    - cron: '0 4 * * *'
  workflow_dispatch:
    inputs:
      force:
        description: '既に発行済みでも再発行する（同日同版は上書き・号数は再利用）'
        type: boolean
        default: false

permissions:
  contents: write

# 朝刊workflowと同じグループ名で直列化する（同時pushの衝突防止）
concurrency:
  group: publish
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Build issue
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FORCE: ${{ inputs.force }}
        run: |
          ARGS=""
          if [ "$FORCE" = "true" ]; then ARGS="--force"; fi
          node scripts/build-issue.mjs midday $ARGS

      - name: Commit and push
        run: |
          git config user.name "YZRS Times 編集部"
          git config user.email "actions@users.noreply.github.com"
          git add public/data data
          if git diff --cached --quiet; then
            echo "本日の昼刊は発行済み。変更なし。"
            exit 0
          fi
          MSG=$(cat data/last-issue-message.txt 2>/dev/null || echo "Publish issue")
          git commit -m "$MSG"
          git push
